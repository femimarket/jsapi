/**
 * app
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import * as runtime from '../runtime';
import { type API } from '../models/API';
import { type ApiAction } from '../models/ApiAction';
import { type ApiAiModel } from '../models/ApiAiModel';
import { type ApiChatMessage } from '../models/ApiChatMessage';
import { type ApiPay } from '../models/ApiPay';
import { type ApiPricing } from '../models/ApiPricing';
import { type ApiStatus } from '../models/ApiStatus';
export interface ApiRequest {
    action: ApiAction;
    audio: string;
    balance: number;
    credit: number;
    file: string;
    id: string;
    image: string;
    messages: Array<ApiChatMessage>;
    model: ApiAiModel;
    pay: ApiPay;
    pricing: ApiPricing;
    prompt: string;
    requestId: string;
    status: ApiStatus;
    userId: string;
}
export interface MediaGateRequest {
    file: string;
}
/**
 * ApiApi - interface
 *
 * @export
 * @interface ApiApiInterface
 */
export interface ApiApiInterface {
    /**
     * Creates request options for api without sending the request
     * @param {ApiAction} action
     * @param {string} audio filename of already uploaded audio else default
     * @param {number} balance
     * @param {number} credit
     * @param {string} file filename of result to retrieve
     * @param {string} id uuid v7
     * @param {string} image filename of already uploaded image else default
     * @param {Array<ApiChatMessage>} messages default value is non-empty array
     * @param {ApiAiModel} model
     * @param {ApiPay} pay
     * @param {ApiPricing} pricing
     * @param {string} prompt
     * @param {string} requestId transient, managed by server
     * @param {ApiStatus} status
     * @param {string} userId
     * @throws {RequiredError}
     * @memberof ApiApiInterface
     */
    apiRequestOpts(requestParameters: ApiRequest): Promise<runtime.RequestOpts>;
    /**
     *
     * @param {ApiAction} action
     * @param {string} audio filename of already uploaded audio else default
     * @param {number} balance
     * @param {number} credit
     * @param {string} file filename of result to retrieve
     * @param {string} id uuid v7
     * @param {string} image filename of already uploaded image else default
     * @param {Array<ApiChatMessage>} messages default value is non-empty array
     * @param {ApiAiModel} model
     * @param {ApiPay} pay
     * @param {ApiPricing} pricing
     * @param {string} prompt
     * @param {string} requestId transient, managed by server
     * @param {ApiStatus} status
     * @param {string} userId
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ApiApiInterface
     */
    apiRaw(requestParameters: ApiRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<API>>;
    /**
     */
    api(requestParameters: ApiRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<API>;
    /**
     * Creates request options for mediaGate without sending the request
     * @param {string} file media filename in _upload
     * @throws {RequiredError}
     * @memberof ApiApiInterface
     */
    mediaGateRequestOpts(requestParameters: MediaGateRequest): Promise<runtime.RequestOpts>;
    /**
     * `#[utoipa::path]` here is doc-only — the fn runs as `from_fn` middleware, not a route handler, but the attribute still emits the spec entry so the generated clients get a typed `downloadMedia(file)`. `servers(...)` pins it to femi.market (the global server is api.earnfemi.com); `Request`/`Next` args aren\'t documentable extractors so utoipa ignores them and uses the explicit `params`.
     * @summary Auth + per-GB debit gate in front of the `_upload` ServeDir on femi.market.   - not a media file → pass through (static site, app data, 404), free   - media file that isn\'t a real file in `_upload` → pass through, free   - media file in `_upload` → require Bearer, charge `size × pricing.gb`,     record a debit in the astc ledger, then let ServeDir stream the bytes. The gate only authorizes/charges; ServeDir still does the actual file streaming (range requests, mime, etc.).
     * @param {string} file media filename in _upload
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ApiApiInterface
     */
    mediaGateRaw(requestParameters: MediaGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<number>>>;
    /**
     * `#[utoipa::path]` here is doc-only — the fn runs as `from_fn` middleware, not a route handler, but the attribute still emits the spec entry so the generated clients get a typed `downloadMedia(file)`. `servers(...)` pins it to femi.market (the global server is api.earnfemi.com); `Request`/`Next` args aren\'t documentable extractors so utoipa ignores them and uses the explicit `params`.
     * Auth + per-GB debit gate in front of the `_upload` ServeDir on femi.market.   - not a media file → pass through (static site, app data, 404), free   - media file that isn\'t a real file in `_upload` → pass through, free   - media file in `_upload` → require Bearer, charge `size × pricing.gb`,     record a debit in the astc ledger, then let ServeDir stream the bytes. The gate only authorizes/charges; ServeDir still does the actual file streaming (range requests, mime, etc.).
     */
    mediaGate(requestParameters: MediaGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<number>>;
}
/**
 *
 */
export declare class ApiApi extends runtime.BaseAPI implements ApiApiInterface {
    /**
     * Creates request options for api without sending the request
     */
    apiRequestOpts(requestParameters: ApiRequest): Promise<runtime.RequestOpts>;
    /**
     */
    apiRaw(requestParameters: ApiRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<API>>;
    /**
     */
    api(requestParameters: ApiRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<API>;
    /**
     * Creates request options for mediaGate without sending the request
     */
    mediaGateRequestOpts(requestParameters: MediaGateRequest): Promise<runtime.RequestOpts>;
    /**
     * `#[utoipa::path]` here is doc-only — the fn runs as `from_fn` middleware, not a route handler, but the attribute still emits the spec entry so the generated clients get a typed `downloadMedia(file)`. `servers(...)` pins it to femi.market (the global server is api.earnfemi.com); `Request`/`Next` args aren\'t documentable extractors so utoipa ignores them and uses the explicit `params`.
     * Auth + per-GB debit gate in front of the `_upload` ServeDir on femi.market.   - not a media file → pass through (static site, app data, 404), free   - media file that isn\'t a real file in `_upload` → pass through, free   - media file in `_upload` → require Bearer, charge `size × pricing.gb`,     record a debit in the astc ledger, then let ServeDir stream the bytes. The gate only authorizes/charges; ServeDir still does the actual file streaming (range requests, mime, etc.).
     */
    mediaGateRaw(requestParameters: MediaGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<number>>>;
    /**
     * `#[utoipa::path]` here is doc-only — the fn runs as `from_fn` middleware, not a route handler, but the attribute still emits the spec entry so the generated clients get a typed `downloadMedia(file)`. `servers(...)` pins it to femi.market (the global server is api.earnfemi.com); `Request`/`Next` args aren\'t documentable extractors so utoipa ignores them and uses the explicit `params`.
     * Auth + per-GB debit gate in front of the `_upload` ServeDir on femi.market.   - not a media file → pass through (static site, app data, 404), free   - media file that isn\'t a real file in `_upload` → pass through, free   - media file in `_upload` → require Bearer, charge `size × pricing.gb`,     record a debit in the astc ledger, then let ServeDir stream the bytes. The gate only authorizes/charges; ServeDir still does the actual file streaming (range requests, mime, etc.).
     */
    mediaGate(requestParameters: MediaGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<number>>;
}
